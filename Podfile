# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RSSManager' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'RxSwift', '~> 6.5.0'
  pod 'RxCocoa', '~> 6.5.0'
  pod 'SDWebImage', '~>5.0'
  pod 'IQKeyboardManagerSwift'
  pod 'Swinject'
  pod 'FirebaseAuth'
  pod 'FirebaseFirestore'

  # Pods for RSSManager

  target 'RSSManagerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RSSManagerUITests' do
    # Pods for testing
  end

end
